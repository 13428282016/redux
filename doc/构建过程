开发环境

开发环境使用webpack.config.dev.js文件进行打包，该配置生成的bundles具有合并后的源代码映射（即可以在浏览器查看合并前的代码），并且没有进行代码压缩（方便调试），在开发时应该在本地环境启动一台前端资源的服务器，这里的开发环境前端资源服务器
地址是http://t.assets.test.com/js/，线上环境是http://assets.test.com/js/，该服务作用还有实时打包功能，不需要进行手动webpack 命令打包，并且该打包在内存中进行，速度极快，打包生成的文件也是放在内存



内网测试环境

开发环境使用webpack.config.production.js文件进行打包，该配置生成的bundles没有合并后的源代码映射（防止别人查看源代码），并且进行代码压缩（加快加载速度），通过gulp生成的js具有压缩代码映射文件（方便在内网对压缩代码进行调试）


外网环境
除了不发表source_maps 文件夹下的文件其他和内网相同